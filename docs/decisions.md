# Decision log

Why things are the way they are. Decision IDs use two digits (`D-01`). Requirement IDs (for example `D-1`, `Q-4`) live in `requirements.md` and are never zero-padded. See consolidation note CN-6.

**How to add one:** append a row to the log with the next ID, today's date, the decision, the why, what was rejected, and the requirement IDs affected. A change to a requirement also needs Justin's approval and a note in `../PROGRESS.md`.

## Log

| ID | Date | Decision | Why | Rejected alternatives | Requirements affected |
|---|---|---|---|---|---|
| D-01 | 2026-09-18 | Everything runs locally on the Pi over the home Wi-Fi. No internet needed at party time. | Reliability and low latency; nothing to break mid-party. | Vercel or other hosting (serverless is poor at WebSockets and adds internet dependence). | R-1, G-1 |
| D-02 | 2026-09-18 | Cloudflare Tunnel is optional, as a second way in. | Fallback if the guest Wi-Fi network isolates clients. Admin routes must stay protected. | Making it the main path. | G-5 |
| D-03 | 2026-09-18 | Camera games and pose tracking are dropped. | The AI HAT+ is too expensive or arrives too late; multi-person pose on the Pi CPU is risky; identity tracking with 16 kids is hard. | Rejected: single-player pose mirror, red light/green light, marker wand. | none (scope dropped) |
| D-04 | 2026-09-18 | The microphone and voice/noise reactions are dropped. | Unreliable in a room of 16 kids; the soundbar under the TV causes feedback; a pad is deterministic. | Rejected: loudness, laughter classification, word spotting, a wireless lavalier mic. | none (scope dropped) |
| D-05 | 2026-09-18 | A wired USB MIDI pad provides 16 special actions. It is read server-side. | Kids like real buttons; avoids browser MIDI permission prompts; wired is more reliable than Bluetooth. | Bluetooth MIDI, browser Web MIDI. | P-1 to P-6, I-1 |
| D-06 | 2026-09-18 | Keyboard keys stand in for the pad during development. A cheap USB keypad is the backup, ordered only if the pad fails the Pi test on 9/20. | Nothing waits on hardware; 6 days of buffer. | Buying a backup up front. | P-6 |
| D-07 | 2026-09-18 | No selfie face cutouts and no photo processing. | Privacy of other people's kids. Characters are simple layered SVGs with attribute options instead. | Rejected: photo cutouts, cloud image generation. | C-1, D-2 |
| D-08 | 2026-09-18 | Characters are data (JSON) on one shared rig; animation clips are written once. | Art volume is the main risk, and any parts combination must work with any clip. | Per-character animation, bitmap sprites. | D-2, D-3, C-10 |
| D-09 | 2026-09-18 | Original Kitty-inspired art only, for private party use. | Sanrio owns the characters. | Tracing or using official art. | Rights (section 6) |
| D-10 | 2026-09-18 | Guest app has two modes (Character, Interact) plus a character strip of up to 3 characters per device. | Lets a parent's phone hold several children's characters. | One character per device. | A-1, A-5, G-6, C-3 |
| D-11 | 2026-09-18 | Fairness, cooldown, and the pending slot are per character, not per device. | Kids without phones must not wait longer than kids with phones. | Per-device seats (unfair to siblings and to tablet users). | Q-3, Q-4, Q-5, I-3, I-7 |
| D-12 | 2026-09-18 | Device timeout by an admin is the main anti-spam enforcement, with a device flood guard of about 1 press per second as a safety net. | Simple, humane, and visible in the admin device list. | Automatic penalties for pressing during cooldown. | Q-12, Q-14, AD-9 |
| D-13 | 2026-09-18 | Queue rules: one pending action per character; expiry about 6 s; cooldown starts when the action plays; least-recently-played character first; the next action is locked. | Bounds the queue, avoids stale actions, keeps the TV queue display stable. | A plain FIFO queue. | Q-4, Q-5, Q-6, Q-7, Q-13 |
| D-14 | 2026-09-18 | Name filter with one-tap hide, not an approval queue. | Approving 16+ names during the party is a chore. | Full approval queue. | G-4, AD-6 |
| D-15 | 2026-09-18 | Focus mode (locked scenes: Cake time and Gift time) with transition cards. Phones show a dark calm screen; the TV goes calm; failsafe auto-release after 30 minutes. | Keep attention on the birthday girl. | Leaving phones interactive during those moments. | T-1 to T-8, AD-10 |
| D-16 | 2026-09-18 | Game time is open (all 16 actions). | Justin: it would not distract from the games. | Limited or locked. | Scenes (section 5.8) |
| D-17 | 2026-09-18 | Shared admin passphrase, no accounts. | Simplicity; two admins. | Separate logins. | AD-1 |
| D-18 | 2026-09-18 | Develop natively on the Mac, deploy to the Pi at least daily, no Docker. | Docker on macOS cannot pass USB through; the real risk is Pi performance, not logic. | Docker-based development. | Development (section 6) |
| D-19 | 2026-09-18 | Depth bands (front, middle, back by recency of acting) start as characters-only. Layered background art is a stretch. | Adds clarity and room for more characters without a large art cost. | Layered art for every scene. | D-16 |
| D-20 | 2026-09-18 | PNG download is the v1 download. Animated GIF is a stretch. | GIF is its own small project. | GIF first. | C-8, C-9 |
| D-21 | 2026-09-18 | Music and sound effects are supplied by Justin. Placeholders until then, swapped by filename. | Keeps code independent of the final audio. | Bundling final audio. | AU-1, AU-2, B-2 |
| D-22 | 2026-09-18 | Parked for after this party: ESP32 devices, AI voice and "Ask Kitty", GPIO arcade buttons and floor pads. | Not enough time. | Parked, not rejected. Details are in `archive/notes-2026-09-18.md`. | none |
| D-23 | 2026-09-18 | Data is deleted after the party. Approved by Justin 2026-09-18. The deletion itself is still done only with Justin's go-ahead on the day (`AGENTS.md` rule 8), after an optional keepsake copy of `data/`. | Privacy. | Keeping it. | Privacy (section 6) |
| D-24 | 2026-09-18 | Game time is an ordinary scene (`game`): a party-game background that may include a pinata and a pin-the-tail game for show only. No game rules, no admin game pick, no scoreboard or timer. Approved by Justin during the Phase 0 contracts review. | The scenes are about atmosphere and reactions, not game logic. Removes the need for a game-choice message and per-game scenes. | Musical chairs, pin the tail, and pinata as chosen or playable games (admin picks); separate scene ids per game. | Scenes (section 5.8); requirements bumped to v1.1 |
| D-25 | 2026-09-18 | The shared tablet is its own device kind (`kind: shared`): one shared profile holding every kid's character, one character per kid by convention, capped at `maxCharactersShared` (default 20); no "New guest" flow ("Add my character" instead). Phones stay `personal` with up to 3 characters. In Interact mode on the tablet, characters that are waiting, playing, or cooling down are dimmed and cannot be selected. The tablet is identified by a private URL carrying `SHARED_TABLET_KEY`. The tablet is a fallback device. Approved by Justin. | A communal tablet for kids without phones needs every kid's character reachable at once. With "New guest" only the current kid could act. | Keeping the tablet as an ordinary device with "New guest"; a separate roster layout. | G-3, G-6, C-3, A-5 (requirements v1.2) |
| D-26 | 2026-09-18 | Phase 0 review of the contract: all 19 `PROPOSED` details in `contracts.md` were reviewed and approved by Justin, several with changes, and are now marked `APPROVED`. Main outcomes: display and pad use a local-only token (the debug panel connects with the `pad` role); admin passphrase goes only to `POST /api/admin/login` and the WebSocket `login` carries a signed 12 h token, with throttled failed attempts; `reqId` on requests; keepalive 10 s and 25 s, with a 30 s server idle close and a 10 s pong timeout on the display; display fallback after 3 s with a bundled scene; take-turns decided in the guest app; broadcast list endpoint with version stamps; PNG download on the phone with a share-sheet or press-and-hold fallback; `options` toggles for glasses and freckles; action ids (`lights_down`) and `surprise` rules; queue entries (reserved `pad` identity, in memory only); scene configs (cards in `config/cards.json`, closed cue list validated at startup, `music` may be null); held cards end on the next admin cue, scene change, reset, or the focus failsafe; focus and scene state in memory (restart returns to Chilling); `broadcast` sent to the display and a `tv` flag in `broadcastResult`; `DeviceInfo` with `name`, `kind`, and cancellable timeouts; settings with live and file-only values, precedence, and limits; character names run through the name filter (`badName`). | Settles the interfaces before Phases 1 to 5 build against them. | The original proposals were changed where noted (see each section of `contracts.md`). | O-6; all contract sections |
| D-27 | 2026-09-18 | Open items answered by Justin: (1) starting values for cooldown 3 s, expiry 6 s, and mood group cooldown 15 s are kept, with final tuning at the rehearsal (O-2); (2) the TV queue display is a list only for now, and raised hands stay in the backlog (O-4); (3) depth bands start at 6 front, 6 middle, 4 back, to be tuned by experiment in Phase 3, which also decides where characters beyond those totals go (the on-stage cap is about 30 and a phone can hold up to 3 characters); (4) backgrounds have a floor only, with no layered art in v1, and an optional foreground overlay goes to the backlog (O-5). Item (4) follows Justin's "not at the cost of the other requirements". | Keeps the art and scene work inside the schedule; layered art is the kind of polish that can be added per scene later. | Layered background art for every scene in v1; a list plus raised hands on the queue display. | I-3, Q-6, Q-11, D-14, D-15, D-16; D-19 stands |
| D-28 | 2026-09-18 | Stack approved by Justin: Node LTS with TypeScript, `ws`, and Vite. React for the guest and admin apps and plain TypeScript for the display. Characters are DOM SVG, one `<svg>` per character in a positioned wrapper, with CSS transforms; effects on a canvas overlay unless the Phase 0 spike shows DOM effects are fine; canvas or PixiJS for characters if the spike stutters. The pad service language (`mido` or `easymidi`) is still decided on the Pi. | The Pi only serves static files and small WebSocket messages, so the framework runs on phones and its size barely matters on a local network; Justin knows React well, which lowers risk on a 9-day schedule. The display's rendering is the real Pi risk, which the spike tests. | Preact (about 40 KB smaller per phone, not needed here); vanilla TypeScript for the guest and admin apps (awkward for state-heavy UI); Socket.IO instead of `ws`. | R-1, D-1, D-9 |
| D-29 | 2026-09-18 | Code reaches the Pi by `git pull` over HTTPS from the public GitHub repo (`justinkprince/hello-party`). The Pi's clone is at `~/hello-party` and has no GitHub key. The deploy script fetches tags and checks out a named ref, then runs `npm ci`. Nothing is pushed to a remote without Justin's approval. Approved by Justin. | The Pi already has the clone, and checking out a tag keeps the Pi on the last `phase-N-done` build (safe-fallback rule). | `rsync` from the Mac over the LAN (works with the internet down, but does not enforce the tag rule); a bare git repo on the Pi. | R-1 (deploy needs internet, runtime does not); Phase 0 deploy and health-check scripts |
| D-30 | 2026-09-18 | Clarification, not a change: every kid has exactly one character. A phone may hold up to 3 characters (G-6, C-3) only because a parent may have up to 3 kids, each with their own character. The shared tablet holds many kids, one character each (G-3, D-25). No kid ever has several characters. "One character per kid by convention" (D-25) and "a phone can hold up to 3 characters" (D-27) mean this. Stated by Justin; also added to `AGENTS.md` ("Easy to get wrong") and requirements section 2. | The misreading "up to 3 characters per kid" has come up more than once and inflates sizing (16 kids would look like 48 characters). | None. | G-3, G-6, C-3, A-5, C-7, D-25, D-27 |
| D-31 | 2026-09-18 | On-stage cap default is **30** (`onStageCap` 30 with ceiling `onStageCapMax` 30 in `config/`, matching the placeholders in `contracts.md` 5.11). It limits only characters drawn on the TV at once; the number of characters that exist has no global limit (only the per-device limits: phone 3, tablet 20). Rendering choice confirmed: DOM SVG, one `<svg>` per character, with a canvas effects overlay. Provisional: re-run the spike with the real art in Phase 1 and lower the cap if 30 fails. Approved by Justin. Spike evidence (Pi 5, Chromium kiosk, 1920x1080, 26-node placeholder characters, 300 particles, 60 s runs; full numbers in `runbooks/pi-setup.md`): 30 characters 59.9 and 60 fps average, p99 16.8 ms; 38 characters 58.6 fps, p99 33.3 ms; 45 characters 50.4 fps, p99 34 ms; 60 characters 32.6 fps, p99 50 ms. 30 characters with 1200 particles: 59.8 fps, p99 17.3 ms, so the effects overlay is cheap. No throttling (`0x0`), 59.8 C at the end. | 30 is the highest measured count that held 60 fps with a p99 under 25 ms, and it is about twice the expected number of kids. | 24, for more headroom against heavier art; 40, which was not tested and sits past the measured break point. | C-7, D-9, O-1, D-28, D-30 |
| D-32 | 2026-09-19/20 | Audio gate carried forward as a known risk. Pi to TV over HDMI is verified on the dev TV (Chromium plays through PipeWire to the TV's own speakers; the sink survives a reboot, an HDMI replug, and a power pull). The soundbar path is untested because the target TV is not available. Fallback if the soundbar does not work: use the TV's own speakers, without the soundbar. Approved by Justin. Test the soundbar on the target TV when Justin has access to it. | The gate allows "verified, or a fallback path chosen and recorded"; the phase should not wait for a TV that is not on hand. | Wait for the target TV before closing Phase 0; other fallbacks in the phase doc (3.5 mm, USB, or Bluetooth audio) if the TV speakers are also unusable. | AU-1, Phase 0 gate, Phase 6 rehearsal |
| D-33 | 2026-09-20 | Pad service language: Node `easymidi` with TypeScript, in `pad-service/`. Approved by Justin. Evidence on the Pi (scratch folder in `/tmp`): `npm install easymidi` finished with no errors and `require('easymidi').getInputs()` listed `Midi Through:Midi Through Port-0`; `pip install mido python-rtmidi` failed while building `python-rtmidi` ("error while generating package metadata"; cause not investigated). The real pad is not tested yet. | It installs cleanly with no system packages and keeps one runtime and one toolchain on the Pi. | `mido` with `python-rtmidi`, which stays the fallback if the real pad does not work with `easymidi`. | Phase 4 pad service; `AGENTS.md` stack |
| D-34 | 2026-09-20 | Deploy and health-check scripts (Phase 0), on top of D-29. (1) The Pi builds after checkout: `npm ci` then `npm run build` in each app folder listed in `APPS` at the top of `scripts/deploy.sh` (only `web/display` today). (2) The ref is a required argument, a tag or a branch, checked out detached (tag first, then `origin/<branch>`); the script stops if tracked files on the Pi have local changes. It runs `git fetch --tags origin` without `--prune`, because `--prune` with `--tags` also deletes local tags. (3) Deploy restarts nothing in Phase 0, since no service exists yet; Phase 2 and Phase 6 add the restart. (4) The scripts read `PI_HOST`, `PI_USER`, and optional `PI_REPO_DIR` from the environment or the Mac's gitignored `.env` (only those keys; no defaults for host and user); `.env.example` holds blank placeholders. (5) The health check prints OK for an HTTP 2xx within 5 s and FAIL otherwise; its default URL is `http://$PI_HOST:$PORT/api/health`, overridable with `HEALTH_URL`. Items 1 to 5 approved by Justin one at a time; leaving out `--prune` differs from the wording he approved for item 2 and was flagged to him when the script was written. | `dist/` is gitignored, so the Pi must build for it to serve exactly what the ref contains; a required ref means a moving branch is never deployed by accident; the real hosts and users stay out of git. | Building on the Mac and copying `dist/`; tags only; an optional `RESTART_CMD` hook; `--host` and `--user` flags or hardcoded defaults; a richer health check (page contents, services, temperature). | D-29; R-1; Phase 0 gate |
| D-35 | 2026-09-20 | Phase 1 rig, approved by Justin (proposed by the Phase 0 agent; nothing is built yet). Canvas: `viewBox 0 0 100 140`, the proportions of the spike character. Layers, back to front: `leg_l`, `leg_r`, `body`, `outfit`, `accessory`, `arm_l`, `arm_r`, then a `head` group holding `ears`, `hair_back`, `head_base`, `face`, `freckles`, `hair_front`, `glasses`, `bow`. The schema's single `hair` part supplies both `hair_back` and `hair_front`. Bows and hats share the `bow` slot. Accessories sit at neck or chest level. The whole character sits in a `root` group. Pivots: `root` (50,130) for bob and jump; hips (38.5,102) and (61.5,102); shoulders (19.5,70) and (80.5,70); `neck` (50,64) for head tilt. Clips move only these groups, never single shapes. Colors are the CSS variables `--skin`, `--hair`, `--bow`, `--outfit`. Node count: the rig itself is 18 nodes (the `<svg>`, `root`, the 7 body layers, `head`, and its 8 layers), before any art shapes inside the layers. Target 40 or fewer per character, ceiling about 50 (D-9); the spike's placeholder had 26. A change to the layers, their order, or the pivots after clips exist needs a new decision, because every clip depends on them (`design.md`). | Any parts combination has to work with any clip, and each clip is written once. A small set of joints keeps the art volume and the node count down. | Finer joints (elbows, knees, ear pivots): more natural motion, but more nodes and more art. Fewer layers (one hair layer, no accessory layer): fewer nodes, but fewer hairstyles. | D-2, D-3, D-8, D-9 |
| D-36 | 2026-09-20 | The character renderer and clip player live in `shared/character/`, plain TypeScript with no `package.json`. Each app imports it by relative path; an app may need `server.fs.allow` and a `tsconfig` include for `../../shared` (`UNVERIFIED` until run). Approved by Justin. | The guest app (Phase 2) needs the same renderer for the picker preview and PNG download, and clips depend on it, so moving it later would be churn. | Building it in `web/display/src/` and moving it in Phase 2. | D-2, C-8, C-10 |
| D-37 | 2026-09-20 | Parts: one SVG file per part, `assets/parts/<slot>_<name>.svg` (as in `contracts.md` section 7), containing groups named after the D-35 layers (a hairstyle holds both `hair_back` and `hair_front`). Files are bundled as text at build time (`import.meta.glob` with `?raw`, `UNVERIFIED` until run) and the renderer copies each group into the matching layer. Colors are `var(--skin)`, `var(--hair)`, `var(--bow)`, `var(--outfit)` inside the files. The parts list comes from the filenames. Approved by Justin. | Works offline with no request and no server route, which suits the display's bundled-fallback rule; `deploy.sh` already builds. | Fetching parts from `/assets/parts/` at runtime (needs a Phase 2 route, more requests on phones, a separate index file). | D-2, D-9 |
| D-38 | 2026-09-20 | Clips are CSS keyframes on the rig groups, chosen by `data-clip` on the character wrapper. Pivots are set once in CSS from the D-35 coordinates using `transform-box: view-box` and `transform-origin` in viewBox units (`UNVERIFIED` on the Pi's Chromium; the spike used `fill-box` with percentages). Movement across the screen stays with the display (it translates the wrapper); the walk clip animates limbs only. One-shot clips end on `animationend`. Switching clips snaps, with no blending. Approved by Justin. | The spike held 60 fps at 30 characters with CSS keyframes on 7 groups per character; plain CSS is easy for Phase 3 to extend. | JavaScript or Web Animations API clips (blending and control, but more code and CPU, unproven on the Pi). | D-2, D-3, D-9 |
| D-39 | 2026-09-20 | The base character looks human, so a kid can make one that resembles them. Animal ears (cat, bear, bunny) are costume ears in the ears slot, a nod to Kitty; the head itself is human. Default skin is `#c68642`. Kitty flavor comes from round shapes, the bow, and optional whiskers, while proportions and face stay original (D-09). Approved by Justin, who asked for human-looking characters and rejected a white-ish default. | Matches the approved schema (hair, skin tone, freckles, glasses) and D-07: attribute options so any character looks like its owner. | A white-furred cat character where `--skin` is the fur color (closer to Sanrio's design, poor fit for hair, freckles, and glasses). | D-2, D-8, D-9, C-1 |
| D-40 | 2026-09-20 | Skin slider color logic (shared code, used by the gallery now and the guest app in Phase 2): one slider from 0 to 1 over about 5 hand-picked anchor tones, `#c68642` among them, blended between anchors. The stored value is a single hex in `colors.skin`. Blush and freckles are semi-transparent so they show on every tone. No new color variables. Justin judges the anchors by eye in the gallery. Approved by Justin. | Keeps every tone looking like real skin from lightest to darkest. | Changing only the lightness of `#c68642` (extremes turn grey or washed out). | D-2, C-1 |
| D-41 | 2026-09-20 | First parts list, 26 parts: ears 3 (cat, bear, bunny); bows/hats 5 (big bow, small bow, party hat, crown, beanie); hairstyles 6 (short, long straight, pigtails, ponytail, curly puff, bun); outfits 4 (dress, overalls, star t-shirt, hoodie); accessories 4 (scarf, heart necklace, bow tie, star badge); faces 4 (dot eyes with smile, dot eyes with open smile, happy closed eyes, wink). Glasses and freckles are toggles. Optional slots (ears, bow/hat, accessory) can be `none`. Outfits, hair, bows/hats, and skin are recolorable through the four color variables. This is 2 more hairstyles than the phase doc (4). If art runs late, cut hairstyles back to 4 and log it (phase slip rule). Approved by Justin. | Hair matters most for a kid to see themselves; the extra cost is two drawings. | Exactly the phase doc's counts (24 parts, fewer looks). A fifth `--trim` color variable for outfits: not done, since it would change D-35 and the schema. | D-2, D-8, C-1 |
| D-42 | 2026-09-20 | The parts gallery is a second page, `gallery.html`, in `web/display/`, plain TypeScript, listed as a second input in `vite.config.ts` (`UNVERIFIED` until run). `deploy.sh` and `health-check.sh` are unchanged. The spike page stays until the frame rate test is re-run with real characters. Approved by Justin. | The gallery is a dev tool; a page in the display app needs no new dev server or preview port on the Pi. | A separate app folder with its own `package.json` (needs a `deploy.sh` `APPS` change and a second port). | D-2, D-8 |
| D-43 | 2026-09-20 | Drawing rule for every part: flat colors with a bold dark outline. The stroke color is fixed at about `#3a2a2a` (not a variable), the width about 2.5 in the 100-wide viewBox, drawn as a `stroke` on the same shapes as the fills so it adds no nodes. No gradients and no SVG filters (D-9). Shading is limited to a few flat semi-transparent shapes such as blush. Approved by Justin. | Keeps light skin, light hair, and pale scenes readable on the TV in daylight (D-8), and keeps parts drawn at different times looking like one set. | Flat colors with no outlines (softer, but light hair on light skin blurs at TV distance). | D-8, D-9 |
| D-44 | 2026-09-20 | Part ids are `<slot>_<name>` and "none" is a real part with an empty SVG file. ears: `ears_none`, `ears_cat`, `ears_bear`, `ears_bunny`. bow: `bow_none`, `bow_big`, `bow_small`, `bow_party_hat`, `bow_crown`, `bow_beanie`. hair: `hair_short`, `hair_long`, `hair_pigtails`, `hair_ponytail`, `hair_puff`, `hair_bun`. face: `face_smile`, `face_grin`, `face_happy`, `face_wink`. outfit: `outfit_dress`, `outfit_overalls`, `outfit_star_tee`, `outfit_hoodie`. accessory: `acc_none`, `acc_scarf`, `acc_heart`, `acc_bowtie`, `acc_star_badge`. Hair, face, and outfit are required, so they have no none (no bald option). 29 files: 26 drawn parts and 3 empty ones. `contracts.md` section 5.1 example ids are replaced with these in the same commit. Approved by Justin. | Every `parts` key is always present, so the schema, server validation, and renderer need no special case for a missing slot. Ids are stored in every character file, so they are fixed before Phase 2. | An absent slot or `null` meaning none (no empty files, but a rule in the schema, the server, and the renderer). | C-10, D-2 |
| D-45 | 2026-09-20 | Phase 1 build order: a thin slice first. Shared types, rig, skin logic, renderer, the 5 v1 clips, and `gallery.html`, with the base body and head and one part per slot (`face_smile`, `hair_short`, `outfit_dress`, `ears_cat`, `bow_big`, `acc_scarf`) and the rest as empty stubs. Justin gives a yes or no on the style and skin tones; the remaining parts are then drawn in batches by slot; then the `contracts.md` update, the frame rate test with real characters, and the Pi gate. Approved by Justin. | A "no" on style costs about one part per slot to redraw, and the `UNVERIFIED` items (Vite globs, `transform-box: view-box`, `fs.allow`) are checked on the Mac early. | Drawing all 26 parts first (nothing to see until late; a style change means redrawing up to 26 parts). | D-2, D-3, D-8 |
| D-46 | 2026-09-20 | The walk clip is a march, not a side-view swing. The legs take turns lifting about 5 units with a small outward kick (8 degrees) and never swing inward, so they cannot cross; the arm opposite the lifting leg swings out 12 degrees; the body bobs once per step. One cycle is 0.7 s (two steps). Only the CSS of the walk clip changed; the rig (D-35) is untouched. `UNVERIFIED` until seen. Approved by Justin, who found the first version's legs swung inward together and crossed. | The legs of the front-view character are 23 units apart, so a mirrored pendulum swing makes them cross; a lift-and-kick reads as a bouncy step while the display slides the character sideways. | A waddle (both legs swing the same way with a side-to-side tilt: no crossing, but reads less like walking). | D-3 |
| D-47 | 2026-09-20 | The ears slot is removed. The `ears` layer leaves the rig (the head group now holds 7 layers, so the rig is 17 nodes), `parts.ears` leaves the Character schema (`parts` has exactly `bow`, `hair`, `face`, `outfit`, `accessory`), and the parts `ears_none`, `ears_cat`, `ears_bear`, `ears_bunny` are dropped, so the parts list is 23 (was 26). The layer order and pivots otherwise stay as D-35, so no clip changes. Supersedes the ears parts of D-35, D-39 (costume ears), D-41, D-44, and the ears row of `contracts.md` 5.1 (updated in the same commit, with the example ids now the real ones from D-44). Approved by Justin, who did not like the cat ears and asked to simplify. The four `ears_*.svg` files are deleted by Justin with `git rm` (the agent has no delete tool); until then the renderer ignores them and logs a warning. | Simplest option; the rig is new and no stored data uses `parts.ears`, so this is the cheapest time to remove it. | Keeping an empty ears layer and schema key with only `ears_none` (no change to D-35 or the schema, but a dead layer and key for Phase 2 to validate and store). | D-2, D-9, C-1, C-10 |
| D-48 | 2026-09-20 | Three hairstyles typical for boys are added: `hair_buzz` (a thin buzz cut with a high hairline), `hair_spiky` (five spikes and a jagged hairline), and `hair_side_part` (a neat cut with a side part and a thin part line). Hairstyles are now 9 (`hair_short`, `hair_long`, `hair_pigtails`, `hair_ponytail`, `hair_puff`, `hair_bun`, and these three). The parts list stays at 26 (bows/hats 5, hairstyles 9, faces 4, outfits 4, accessories 4) because dropping the ears (D-47) freed 3. Ids follow D-44, which they extend; D-41's hairstyle count (6) is superseded. Approved by Justin, who noticed the first six styles were mostly typical for girls. | A kid should be able to build a character that looks like them (D-07), and the styles drawn so far leaned toward girls. No net increase in drawing. | Adding only two (`hair_spiky`, `hair_side_part`): less drawing, but no true short cut. | D-2, D-8, C-1 |
| D-49 | 2026-09-20 | The hair ties on `hair_pigtails` and `hair_ponytail` are a fixed white with the usual outline, not the `--bow` color. `hair_bun` has no tie (removed earlier, after Justin found its `--bow` colored tie looked like a stray piece). Approved by Justin. | A tie should not change color when the bow color changes, and white shows on any hair color; it matches the other fixed whites (shoes, dress band). | Removing the ties (fewer nodes, but the attachment point is less clear); keeping the `--bow` color (the same coupling Justin flagged on the bun). | D-8, D-43 |
| D-50 | 2026-09-20 | An outfit part may also supply `arm_l` and `arm_r` groups (sleeves). The renderer draws them on top of the base skin arm, so they turn with the arm. Only the outfit slot does this (`SLOT_LAYERS.outfit` is `outfit`, `arm_l`, `arm_r`; the arm layers add to the base art, every other layer belongs to the part alone). The rig, layer order, pivots, and all clips are unchanged. Costs 2 nodes for each outfit with sleeves. Used by `outfit_overalls` (a fixed white shirt with short sleeves), `outfit_star_tee` (short sleeves in `--outfit`), and `outfit_hoodie` (long sleeves in `--outfit`); `outfit_dress` is sleeveless. The tee and hoodie also draw fixed dark shorts (`#3d4f73`) so the torso does not show as bare skin. Approved by Justin. | Without sleeves the tee and hoodie would look sleeveless, and sleeves painted on the torso would stay at the shoulder while the arm rises in wave, jump, and dance. | Sleeves painted as static stubs in the `outfit` layer (no code change, but they visibly separate from the arm in the raised-arm clips). | D-2, D-3, D-35 |
| D-51 | 2026-09-20 | Two Phase 1 test tools. (1) The Phase 0 spike page (`web/display/index.html` and `src/spike.ts`) now draws real characters from `shared/character` and keeps the same measuring, so the numbers compare with Phase 0. New URL parameters: `clip=` (idle, walk, wave, dance_a, jump; default walk) and `worst=1` (the heaviest looks, 46 nodes: beanie, pigtails, grin, overalls, heart, glasses, freckles; otherwise seeded random looks); keys `c` and `l` change clip and looks. Results also record the node range, clip, and looks. (2) The gallery has a TV showcase mode, `gallery.html?tv=1&look=<url-encoded JSON>&bg=<pink, white, grey, sand, dark>`: no controls, one large character playing idle, walk, wave, dance A, and jump in turn (3 s each, looping), with the clip name in the corner. The gallery prints a ready-made link for the current look. The look holds only part ids, colors, and options (no secrets); the JSON must be URL-encoded because a hex color's `#` would cut the URL. Approved by Justin (option A for both). | The gate needs fps with real characters on the Pi, and the Pi has no keyboard or mouse (runs are started by URL over SSH), so the chosen character has to be shown by URL too. | A new `bench.html` (duplicated measuring code); a USB keyboard and mouse on the Pi; showing only the sample character (not custom). | D-31, D-42, Phase 1 gate |

## Consolidation notes

Changes and resolutions made while building the hand-off pack on 2026-09-18.

| # | Note |
|---|---|
| CN-1 | The old Day 5 gate said cake time runs "from the admin cue," but the admin panel is built on Day 6. Resolution: in Phase 4 the cake script is triggered from the debug panel. Phase 5 rewires it to the admin panel and adds the transition card and focus mode. |
| CN-2 | Several requirement IDs were never scheduled in the old timeline: D-10, D-11, D-12, Q-9, Q-10, Q-11, Q-13, G-5. They are now assigned (see `phases/README.md`). Unlisted IDs were assigned by judgment: D-8 to Phase 1 (art style), D-12 to Phase 6, D-4 to Phase 4, D-1 to Phase 0, C-2 to Phase 2. |
| CN-3 | Bot mode appeared on Day 4 (gate) and Day 6 (demo). Resolution: Phase 3 builds a minimal spawner used by tests and gates; Phase 5 builds the demo mode. |
| CN-4 | Phase 0 sets up initial systemd and kiosk services; Phase 6 hardens them (R-2, R-3). |
| CN-5 | The old Day 4 clip list omitted "clap," but D-3 lists it. Added "clap" to Phase 3. |
| CN-6 | Decision IDs (`D-01` to `D-23`) share a letter with requirement IDs (`D-1` to `D-16`). Kept as specified; decisions are always two-digit. Decision D-15 (focus mode) is not requirement D-15 (raised hands), and decision D-16 (Game time open) is not requirement D-16 (depth bands). |
| CN-7 | Requirement IDs that span phases have one owning phase in the coverage table (the phase that completes them). Partial work is listed in each phase doc under "Requirements covered". |
| CN-8 | The old "Order today" checkboxes moved into Phase 0, except the conditional USB keypad order, which moved to Phase 2 where the pad test happens. |
| CN-9 | The two Day 9 checkboxes (morning setup, reset to starting scene) have no phase, because Day 9 is the party. They live in `runbooks/party-day.md`. The "every checkbox in exactly one phase doc" check counts 60 of 62 old checkboxes in phase docs; the other 2 are in that runbook. |
| CN-10 | The old timeline's "Dropped:" line and scope tiers were removed from `timeline.md`. Dropped scope lives in decisions D-03, D-04, D-07; priorities live in `requirements.md`. |
| CN-11 | `requirements.md` was moved unchanged plus a header. Its "Companion docs" line still names `notes.md` and `timeline.md` at the old root paths. Left as is because the file is frozen. |
| CN-12 | Task lists in phase docs include a few tasks not in the old timeline, marked "(from requirements)", so that every requirement assigned to a phase has a task. |
| CN-13 | The agent that built this pack had file access but no shell, so it could not run git or script-based checks. Git history was left for Justin to create from a short command list (see `../PROGRESS.md`). The old root `timeline.md` stays until that step removes it. |

## Open

Items from `requirements.md` section 9, plus two added by the pack.

| # | Open item | Decide by | Requirement IDs |
|---|---|---|---|
| O-1 | Done 2026-09-18: on-stage cap default 30, provisional until re-tested with real art in Phase 1 (D-31). | Phase 0 | C-7 |
| O-2 | Starting values confirmed 2026-09-18 (D-27): cooldown 3 s, expiry 6 s, mood group cooldown 15 s. Final tuning at the rehearsal. | Fri 9/25 | I-3, Q-6, Q-11 |
| O-3 | Done 2026-09-18: guest and character data is deleted after the party (D-23). | Before 9/26 | Privacy |
| O-4 | Done 2026-09-18: list only for now (D-27). | Phase 5 | D-14, D-15 |
| O-5 | Partly decided (D-27): backgrounds are floor only. Band sizes start at 6 / 6 / 4 and are tuned by experiment in Phase 3. | Phase 3 | D-16 |
| O-6 | Done 2026-09-18: all 19 `PROPOSED` details in `contracts.md` reviewed and approved (see D-26). | Phase 0 | all |
| O-7 | Done 2026-09-18: stack confirmed (D-28). The pad service language is still chosen on the Pi. | Phase 0 | R-1, D-1, D-9 |
